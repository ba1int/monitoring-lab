define service {
    use                 lab-service
    hostgroup_name      lab-hosts
    service_description SSH reachability
    check_command       check-ssh-service
}

define service {
    use                 lab-service
    hostgroup_name      lab-hosts
    service_description Middleware health
    check_command       check-lab-middleware-by-ssh
}
