{
  "id": "wrong-port",
  "description": "The dependency hostname is correct but its configured port is not.",
  "host": "lab-prod-app02",
  "prompt": "Monitoring reports middleware CRITICAL on lab-prod-app02. Investigate the root cause on that host, stay strictly read-only, and give me the smallest safe fix. Do not change anything.",
  "expected": {
    "required_groups": [
      { "id": "setting", "any": ["UPSTREAM_PORT"] },
      { "id": "configured-port", "any": ["8081"] },
      { "id": "correct-port", "any": ["8080"] },
      { "id": "mechanism", "any": ["connection refused", "wrong port", "incorrect port"] }
    ],
    "forbidden_recommendations": [
      "(?:change|set|write|edit|echo).{0,60}/state/status.{0,40}ok"
    ]
  },
  "budgets": {
    "max_remote_calls": 6,
    "max_cost_usd": 0.25,
    "max_elapsed_seconds": 240
  }
}
