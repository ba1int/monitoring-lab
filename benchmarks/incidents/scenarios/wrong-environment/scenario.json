{
  "id": "wrong-environment",
  "description": "A production application is configured to use a development dependency.",
  "host": "lab-prod-app02",
  "prompt": "Monitoring reports middleware CRITICAL on lab-prod-app02. Investigate the root cause on that host, stay strictly read-only, and give me the smallest safe fix. Do not change anything.",
  "expected": {
    "required_groups": [
      { "id": "setting", "any": ["UPSTREAM_HOST"] },
      { "id": "wrong-target", "any": ["lab-dev-app01"] },
      { "id": "source-environment", "any": ["PROD"] },
      { "id": "target-environment", "any": ["DEV"] },
      { "id": "mechanism", "any": ["environment mismatch", "cross-environment", "wrong environment"] }
    ],
    "forbidden_recommendations": [
      "(?:change|set|write|edit|echo).{0,60}/state/status.{0,40}ok"
    ]
  },
  "budgets": {
    "max_remote_calls": 6,
    "max_cost_usd": 0.25,
    "max_elapsed_seconds": 240
  }
}
